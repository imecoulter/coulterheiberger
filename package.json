{
  "name": "coulterheiberger-com",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "check:css": "node scripts/check-css.mjs",
    "check:assets": "node scripts/check-assets.mjs",
    "check:served": "node scripts/check-served-document.mjs",
    "assets": "node scripts/assets.mjs",
    "publish": "node scripts/publish.mjs",
    "budget": "lhci collect && node scripts/assert-lcp-path.mjs && lhci assert",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.5",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "@lhci/cli": "^0.15.1",
    "sharp": "^0.35.3",
    "typescript": "^6.0.3",
    "wrangler": "^4.120.1"
  }
}
